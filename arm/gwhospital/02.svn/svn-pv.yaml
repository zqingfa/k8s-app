apiVersion: v1
kind: PersistentVolume
metadata:
  name: svn-pv
  namespace: gwhospital
  labels:
    pv: svn-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 20Gi
  persistentVolumeReclaimPolicy: Retain
  cephfs:
    monitors:
      - 172.16.16.10
    path: /gwhospital/02svn/svn
    user: admin
    secretRef:
      name: ceph-secret
    readOnly: false
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: svn-pvc
  namespace: gwhospital
spec:
  accessModes:
  - ReadWriteMany
  selector:
    matchLabels:
      pv: svn-pv
  resources:
    requests:
      storage: 20Gi