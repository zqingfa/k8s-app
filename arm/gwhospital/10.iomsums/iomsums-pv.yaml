apiVersion: v1
kind: PersistentVolume
metadata:
  name: iomsums-pv
  namespace: gwhospital
  labels:
    pv: iomsums-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 20Gi
  persistentVolumeReclaimPolicy: Retain
  cephfs:
    monitors:
      - k8s-master
    path: /gwhospital/10iomsums/data
    user: admin
    secretRef:
      name: ceph-secret
    readOnly: false
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: iomsums-pvc
  namespace: gwhospital
spec:
  accessModes:
  - ReadWriteMany
  selector:
    matchLabels:
      pv: iomsums-pv
  resources:
    requests:
      storage: 20Gi