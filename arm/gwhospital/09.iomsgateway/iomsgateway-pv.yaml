apiVersion: v1
kind: PersistentVolume
metadata:
  name: iomsgateway-pv
  namespace: gwhospital
  labels:
    pv: iomsgateway-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 20Gi
  persistentVolumeReclaimPolicy: Retain
  cephfs:
    monitors:
      - k8s-master
    path: /gwhospital/09iomsgateway/data
    user: admin
    secretRef:
      name: ceph-secret
    readOnly: false
---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: iomsgateway-pvc
  namespace: gwhospital
spec:
  accessModes:
  - ReadWriteMany
  selector:
    matchLabels:
      pv: iomsgateway-pv
  resources:
    requests:
      storage: 20Gi